apiVersion: v1
kind: Service
metadata:
  name: auspice-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    ms: auspice

---

apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: auspice-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        ms: auspice
    spec:
      containers:
      - name: auspice
        image: staphb/auspice
        command: ["auspice", "view"]
        ports:
        - containerPort: 80

