apiVersion: v1
kind: Service
metadata:
  name: sample-status-db-svc
spec:
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    app: sample-status

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sample-status-db-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
  - ReadWriteMany

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-status-db-deployment
spec:
  selector:
    matchLabels:
      app: sample-status
  replicas: 1
  template:
    metadata:
      labels:
        app: sample-status
    spec:
      containers:
      - name: mongodb
        image: mongo
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: data
          mountPath: /data/db
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sample-status-db-pvc